{
  "config": {
    "step": {
      "user": {
        "title": "HA Complete Irrigation Integration",
        "description": "Click Submit to install. Full setup wizard (zones, sensors, weather, schedules) runs after install."
      }
    },
    "abort": {
      "already_configured": "Complete Irrigation is already configured."
    }
  }
}
